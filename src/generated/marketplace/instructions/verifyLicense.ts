/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import { findPurchasePda } from "../pdas";
import { MARKETPLACE_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const VERIFY_LICENSE_DISCRIMINATOR = new Uint8Array([
  244, 67, 31, 199, 228, 161, 38, 192,
]);

export function getVerifyLicenseDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VERIFY_LICENSE_DISCRIMINATOR,
  );
}

export type VerifyLicenseInstruction<
  TProgram extends string = typeof MARKETPLACE_PROGRAM_ADDRESS,
  TAccountBuyer extends string | AccountMeta<string> = string,
  TAccountAsset extends string | AccountMeta<string> = string,
  TAccountPurchase extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBuyer extends string
        ? ReadonlyAccount<TAccountBuyer>
        : TAccountBuyer,
      TAccountAsset extends string
        ? ReadonlyAccount<TAccountAsset>
        : TAccountAsset,
      TAccountPurchase extends string
        ? ReadonlyAccount<TAccountPurchase>
        : TAccountPurchase,
      ...TRemainingAccounts,
    ]
  >;

export type VerifyLicenseInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type VerifyLicenseInstructionDataArgs = {};

export function getVerifyLicenseInstructionDataEncoder(): FixedSizeEncoder<VerifyLicenseInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: VERIFY_LICENSE_DISCRIMINATOR }),
  );
}

export function getVerifyLicenseInstructionDataDecoder(): FixedSizeDecoder<VerifyLicenseInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getVerifyLicenseInstructionDataCodec(): FixedSizeCodec<
  VerifyLicenseInstructionDataArgs,
  VerifyLicenseInstructionData
> {
  return combineCodec(
    getVerifyLicenseInstructionDataEncoder(),
    getVerifyLicenseInstructionDataDecoder(),
  );
}

export type VerifyLicenseAsyncInput<
  TAccountBuyer extends string = string,
  TAccountAsset extends string = string,
  TAccountPurchase extends string = string,
> = {
  buyer: Address<TAccountBuyer>;
  asset: Address<TAccountAsset>;
  purchase?: Address<TAccountPurchase>;
};

export async function getVerifyLicenseInstructionAsync<
  TAccountBuyer extends string,
  TAccountAsset extends string,
  TAccountPurchase extends string,
  TProgramAddress extends Address = typeof MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: VerifyLicenseAsyncInput<
    TAccountBuyer,
    TAccountAsset,
    TAccountPurchase
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  VerifyLicenseInstruction<
    TProgramAddress,
    TAccountBuyer,
    TAccountAsset,
    TAccountPurchase
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    buyer: { value: input.buyer ?? null, isWritable: false },
    asset: { value: input.asset ?? null, isWritable: false },
    purchase: { value: input.purchase ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.purchase.value) {
    accounts.purchase.value = await findPurchasePda({
      buyer: expectAddress(accounts.buyer.value),
      asset: expectAddress(accounts.asset.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.buyer),
      getAccountMeta(accounts.asset),
      getAccountMeta(accounts.purchase),
    ],
    data: getVerifyLicenseInstructionDataEncoder().encode({}),
    programAddress,
  } as VerifyLicenseInstruction<
    TProgramAddress,
    TAccountBuyer,
    TAccountAsset,
    TAccountPurchase
  >);
}

export type VerifyLicenseInput<
  TAccountBuyer extends string = string,
  TAccountAsset extends string = string,
  TAccountPurchase extends string = string,
> = {
  buyer: Address<TAccountBuyer>;
  asset: Address<TAccountAsset>;
  purchase: Address<TAccountPurchase>;
};

export function getVerifyLicenseInstruction<
  TAccountBuyer extends string,
  TAccountAsset extends string,
  TAccountPurchase extends string,
  TProgramAddress extends Address = typeof MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: VerifyLicenseInput<TAccountBuyer, TAccountAsset, TAccountPurchase>,
  config?: { programAddress?: TProgramAddress },
): VerifyLicenseInstruction<
  TProgramAddress,
  TAccountBuyer,
  TAccountAsset,
  TAccountPurchase
> {
  // Program address.
  const programAddress = config?.programAddress ?? MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    buyer: { value: input.buyer ?? null, isWritable: false },
    asset: { value: input.asset ?? null, isWritable: false },
    purchase: { value: input.purchase ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.buyer),
      getAccountMeta(accounts.asset),
      getAccountMeta(accounts.purchase),
    ],
    data: getVerifyLicenseInstructionDataEncoder().encode({}),
    programAddress,
  } as VerifyLicenseInstruction<
    TProgramAddress,
    TAccountBuyer,
    TAccountAsset,
    TAccountPurchase
  >);
}

export type ParsedVerifyLicenseInstruction<
  TProgram extends string = typeof MARKETPLACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    buyer: TAccountMetas[0];
    asset: TAccountMetas[1];
    purchase: TAccountMetas[2];
  };
  data: VerifyLicenseInstructionData;
};

export function parseVerifyLicenseInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedVerifyLicenseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      buyer: getNextAccount(),
      asset: getNextAccount(),
      purchase: getNextAccount(),
    },
    data: getVerifyLicenseInstructionDataDecoder().decode(instruction.data),
  };
}
